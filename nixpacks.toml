[phases.setup]
nixPkgs = ["php84", "php84Packages.composer", "nginx", "nodejs_20"]

[phases.build]
cmds = [
    "npm run build",
    "php artisan config:cache",
    "php artisan route:cache",
    "php artisan view:cache"
]

